import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cognicare.caregiver',
  appName: 'CogniCare-Caregiver',
  webDir: 'dist'
};

export default config;
